HashMap<EntityPlayer, renderBlock> list = new HashMap();

List<EntityPlayer> playerList = playerEntities;
for (EntityPlayer player : list.keySet()) {
    if (!playerList.contains(player)) 
        list.remove(player);
    }
}
for (EntityPlayer player : playerList) {
    if (list.containsKey(player)) {
        list.get(player).render();
    }
}

用string ?
renderBlock {
    EntityPlayer player -> String playerName
}

class renderBlock {
    private final BlockPos pos;
    private final String name;
    private float mineDamage
    
    void renderBlock(BlockPos pos, String name) {
        this.pos = pos;
        this.name = name;
        this.mineDamage = 0;
    }
    
    void render() {
        mineDamage += getStrengh()
        renderBox() 
    }
}
